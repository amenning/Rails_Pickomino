# Rename this file secrets.yml after entering secrets generated from rake secret

test:
  secret_key_base:
  secret_token:

development:
  secret_key_base:  
  secret_token:

production:
  secret_key_base:  # (Heroku) Set by an environment variable in production.rb 
  secret_token:  # (Heroku) Set by an environment variable in production.rb 
